# Server Configuration
server.port=8080

# Application Configuration
app.name=Java CI/CD Application
app.version=1.0.0

# Docker Configuration
docker.registry=your-dockerhub-username
docker.image.name=java-app

# Kubernetes Configuration
k8s.namespace=default
k8s.replicas=2
